The home of our recognition model and search method. Our training logic and checkpoints will live here.

search.py has find_solution() which given a puzzle and an LLM, uses the LLM to find a python program that solves the problem.

train.py includes the train loop and fine tuning logic.